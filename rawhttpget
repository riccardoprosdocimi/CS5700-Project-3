#!/usr/bin/env python3

from tcp_sock import TCPSocket


def main():
    tcp_socket = TCPSocket(dst_host="david.choffnes.com")
    if tcp_socket.handshake():
       print("Handshake succeeded!") 


if __name__ == "__main__":
    main()
