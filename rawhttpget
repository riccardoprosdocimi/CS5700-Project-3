#!/usr/bin/env python3

from sys import argv, exit
from http_get import download


def main():
    if len(argv) != 2:
        print("Please provide destination hostname")
        exit(1)
    
    url = argv[1]
    download(url=url)


if __name__ == "__main__":
    main()
